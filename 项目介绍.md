# 待办事项（TodoList）项目介绍

## 一、这个项目是什么？

这是一个**待办事项管理应用**——你可以用它来记录自己要做的事情，比如"完成作业"、"买牛奶"、"准备周报"等等。

它的外观简洁，功能完整，支持分组、标签、搜索、深色模式、数据导入导出等，打开浏览器就能直接使用，不需要安装任何东西。

---

## 二、项目文件结构

整个项目只有 **4 个文件**，非常精简：

```
Todolistbytrae/
├── index.html      ← 主页面（任务列表页）
├── add-task.html   ← 新增任务页（备用入口）
├── script.js       ← 所有逻辑代码（JavaScript）
└── styles.css      ← 所有样式代码（CSS）
```

| 文件 | 作用 | 通俗理解 |
|------|------|----------|
| `index.html` | 主页面，展示任务列表、搜索栏、弹窗等 | 房子的结构图纸 |
| `add-task.html` | 独立的新增任务页面 | 另一个入口的房间 |
| `script.js` | 所有功能逻辑：增删改查、搜索、存储等 | 房子里各种电器的开关和电路 |
| `styles.css` | 所有外观样式：颜色、布局、动画、深色模式等 | 房子的装修和壁纸 |

---

## 三、技术栈——用了哪些技术？

本项目是**纯前端项目**，没有使用任何框架或构建工具：

| 类别 | 使用的技术 | 没有使用的（你可能听过的） |
|------|-----------|--------------------------|
| 页面结构 | HTML5 | - |
| 样式 | CSS3 | Tailwind、Sass |
| 逻辑 | JavaScript (ES6+) | React、Vue、TypeScript |
| 构建工具 | 无 | Webpack、Vite |
| 包管理器 | 无 | npm、yarn |
| 数据存储 | 浏览器 localStorage | 数据库、服务器 |

**这意味着什么？**

- 不需要 `npm install` 安装依赖，双击 `index.html` 就能运行
- 不需要 Node.js、不需要命令行、不需要编译
- 代码完全可读，入门门槛极低

---

## 四、核心功能一览

### 4.1 任务管理（增删改查）

| 功能 | 说明 |
|------|------|
| 添加任务 | 点击"添加任务"按钮，弹出模态框填写任务名称、分组、标签 |
| 编辑任务 | 点击任务右侧"编辑"按钮，弹出模态框修改内容 |
| 删除任务 | 点击"删除"按钮，弹出确认对话框后删除（带滑出动画） |
| 完成任务 | 勾选复选框，任务文字加删除线，透明度降低 |

### 4.2 分组与标签

- **分组**（左侧彩色边框区分）：工作（橙色）、学习（青色）、生活（紫色）
- **标签**：重要、紧急、日常

### 4.3 搜索与筛选

- 顶部搜索栏：实时搜索任务内容、分组、标签
- 下拉筛选：按分组或标签过滤显示
- 快捷键：按 `/` 键快速聚焦搜索框

### 4.4 深色模式

- 右上角月亮/太阳图标切换
- 主题偏好保存在浏览器中，下次打开自动恢复

### 4.5 数据管理

| 功能 | 说明 |
|------|------|
| 自动保存 | 每 2 秒自动保存一次到浏览器本地 |
| 自动备份 | 每保存 5 次自动创建备份 |
| 恢复备份 | 从上次备份恢复数据 |
| 导出任务 | 将任务导出为 JSON 文件下载到本地 |
| 导入任务 | 从 JSON 文件导入任务 |

### 4.6 统计信息

- 显示任务总数、已完成数、剩余数、完成百分比
- 进度条可视化完成率

### 4.7 键盘快捷键

| 快捷键 | 功能 |
|--------|------|
| `/` | 聚焦搜索框 |
| `Escape` | 清空搜索/关闭输入 |
| `Ctrl + Enter` | 快速添加任务 |
| `Ctrl + Shift + X` | 切换任务完成状态 |

---

## 五、代码架构详解

### 5.1 整体设计模式

整个 JavaScript 代码被组织成一个名为 `TodoApp` 的**对象字面量**：

```
TodoApp = {
    init()           → 初始化入口
    elements         → 缓存 DOM 元素引用
    data             → 分组和标签的配置数据
    bindEvents()     → 绑定所有事件监听
    ...其他方法       → 各个功能模块
}
```

应用启动方式：

```javascript
document.addEventListener('DOMContentLoaded', () => {
    TodoApp.init();
});
```

即页面加载完成后，自动调用 `init()` 方法启动整个应用。

### 5.2 初始化流程（init 方法）

`init()` 做了四件事：

1. **获取 DOM 元素**：把页面上的输入框、按钮、列表等元素缓存到 `this.elements` 中，避免重复查找
2. **配置数据**：在 `this.data` 中定义分组（工作/学习/生活）和标签（重要/紧急/日常）
3. **绑定事件**：调用 `bindEvents()` 为所有按钮、输入框绑定点击/按键事件
4. **初始化组件**：调用 `initializeComponents()` 初始化下拉选择器、主题、数据管理按钮等

然后根据当前页面类型：
- **主页面**（index.html）：加载任务列表 + 开启自动保存
- **新增任务页**（add-task.html）：设置添加任务的事件

### 5.3 功能模块划分

`script.js` 中的方法按功能可以分为以下模块：

#### 📌 任务 CRUD

| 方法 | 功能 |
|------|------|
| `createTaskElement()` | 创建一个任务 DOM 元素（含复选框、文字、标签、编辑/删除按钮） |
| `addTask()` | 从 add-task 页面添加任务 |
| `handleModalSubmit()` | 处理模态框提交（新增或编辑） |
| `updateTask()` | 更新任务内容和标签 |
| `deleteTaskElement()` | 删除任务（带滑出动画） |

#### 📌 搜索与筛选

| 方法 | 功能 |
|------|------|
| `searchTasks()` | 按关键词搜索 |
| `filterTasks()` | 按分组和标签筛选 |

#### 📌 数据持久化

| 方法 | 功能 |
|------|------|
| `saveTasks()` | 保存所有任务到 localStorage |
| `loadTasks()` | 从 localStorage 加载任务并渲染到页面 |
| `getAllTasks()` | 从 DOM 中收集所有任务数据 |
| `setupAutoSave()` | 设置 2 秒定时自动保存 |

#### 📌 数据管理

| 方法 | 功能 |
|------|------|
| `createBackup()` | 创建备份副本 |
| `restoreFromBackup()` | 从备份恢复 |
| `exportTasks()` | 导出为 JSON 文件 |
| `importTasks()` | 从 JSON 文件导入 |

#### 📌 UI 组件

| 方法 | 功能 |
|------|------|
| `showConfirm()` | 弹出确认对话框（Promise 风格） |
| `updateEmptyState()` | 空任务时显示提示 |
| `updateTaskStats()` | 更新统计信息和进度条 |
| `addClearCompletedButton()` | 添加"清空已完成"按钮 |
| `addDataManagementButtons()` | 添加数据管理按钮组 |

#### 📌 主题

| 方法 | 功能 |
|------|------|
| `toggleTheme()` | 切换深色/浅色模式 |
| `applySavedTheme()` | 启动时读取保存的主题偏好 |

#### 📌 快捷键

| 方法 | 功能 |
|------|------|
| `addKeyboardShortcuts()` | 注册全局键盘快捷键 |

### 5.4 数据存储结构

数据存储在浏览器的 `localStorage` 中，格式如下：

```json
{
    "version": "1.0",
    "lastUpdated": "2026-05-02T10:30:00.000Z",
    "tasks": [
        {
            "id": "1746172200000",
            "text": "完成周报",
            "group": "工作",
            "tag": "重要",
            "completed": false
        },
        {
            "id": "1746172201000",
            "text": "买牛奶",
            "group": "生活",
            "tag": "日常",
            "completed": true
        }
    ]
}
```

`localStorage` 是浏览器内置的键值对存储，数据保存在用户本地，关闭浏览器也不会丢失（除非手动清除）。

### 5.5 CSS 架构

`styles.css` 的组织方式：

1. **全局重置**（`*` 选择器）：统一 margin、padding、box-sizing
2. **基础样式**（`body`）：字体、最大宽度、背景色、过渡动画
3. **深色模式**（`body.dark-mode`）：通过在 body 上添加/移除 `dark-mode` 类实现
4. **组件样式**：按页面区域逐一定义
   - 头部（`.header-container`）
   - 搜索栏（`.search-container`）
   - 按钮组（`.button-container`）
   - 任务列表（`#taskList`、`.task-item`）
   - 任务标签（`.group-tag`）
   - 统计栏（`.task-stats`）
   - 数据管理（`.data-management`）
   - 模态框（`.modal`）
   - 空状态（`.empty-state`）
5. **动画**（`@keyframes`）：`fadeIn`、`modalSlideIn`
6. **响应式**（`@media`）：768px 和 480px 两个断点适配移动端

---

## 六、两个页面的关系

项目有两个 HTML 页面：

- **index.html**：主页面，包含完整的任务列表、搜索、筛选、模态框编辑等功能
- **add-task.html**：独立的新增任务页面，添加完成后跳转回主页面

实际上，主页面已经通过模态框（modal）实现了添加任务功能，`add-task.html` 是一个独立的备用入口。两个页面共用同一个 `script.js` 和 `styles.css`，通过检测 `window.location.pathname` 来区分当前页面，执行不同的初始化逻辑。

---

## 七、前端基础知识速览

如果你完全没有接触过前端，以下概念帮助你快速理解本项目的代码：

### HTML — 页面的骨架

HTML 定义了页面上**有哪些元素**。比如：

```html
<button id="addTaskBtn">添加任务</button>
```

这行代码的意思是：在页面上放一个按钮，按钮上写着"添加任务"，它的 ID 是 `addTaskBtn`（方便 JavaScript 找到它）。

### CSS — 页面的皮肤

CSS 定义了元素**长什么样子**。比如：

```css
button {
    background-color: #6d4c41;
    color: white;
    border-radius: 10px;
}
```

这行代码的意思是：所有按钮的背景色是棕色，文字是白色，圆角 10px。

### JavaScript — 页面的大脑

JavaScript 让页面**动起来**。比如：

```javascript
document.getElementById('addTaskBtn').addEventListener('click', () => {
    // 点击"添加任务"按钮后执行的操作
});
```

这行代码的意思是：找到 ID 为 `addTaskBtn` 的按钮，当用户点击它时，执行花括号里的代码。

### localStorage — 浏览器的记事本

`localStorage` 是浏览器提供的小型本地存储：

```javascript
localStorage.setItem('tasks', JSON.stringify(tasks));  // 存数据
localStorage.getItem('tasks');                           // 取数据
```

数据存在用户浏览器中，不涉及任何服务器，关闭浏览器再打开数据还在。

---

## 八、如何运行

1. 双击 `index.html` 文件，用浏览器打开即可
2. 或者用 VS Code 的 Live Server 插件右键打开
3. 不需要安装 Node.js，不需要 `npm install`，不需要任何命令行操作

---

## 九、可能的改进方向

如果你想继续学习或扩展这个项目，以下是一些思路：

| 方向 | 说明 |
|------|------|
| 使用框架 | 用 React 或 Vue 重写，学习组件化开发 |
| 添加后端 | 用 Node.js + 数据库实现多设备同步 |
| 拖拽排序 | 引入拖拽库实现任务排序 |
| 截止日期 | 给任务添加截止日期和提醒 |
| 多语言 | 支持中英文切换 |
| 单元测试 | 用 Jest 为 JS 方法编写自动化测试 |
